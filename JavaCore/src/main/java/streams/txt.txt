Создание стрима:
1 cпособ - из коллекции путем вызова метода stream()
2 способ - Arrays.stream()
3 способ - создать стрим с 0
Stream<Integer> stream1 = Stream.of(1,2,3,4,5,1,2,3);

//filter это интерфейс Predicate (он нужен чтобы отфильтровать элементы в коллекции по определенному условию/критерию)

// map это интерфейс Function (Student -> Double)

//collect - нужен чтобы преобразовать поток в коллекцию

//toArray() - нужен чтобы преобразовать поток в массив

//forEach - возвращает void, collect после сделать уже не сможем, можно использовать чтобы пройтись по всем элементам коллекции
System.out::println - это называется метод-референс
в параметр println нужно вставить каждый элемент нашего стрима

//reduce
на выходе получаем 1 элемент
get()
getAsInt()
например, хотим получить произведение всех элементов
можно оборачивать элемент на выходе в Optional - проверять лист на NullPointerExeption

Optional можно использовать например при фильтрации элементов


//метод sorted
для сортировки, то же самое что и компаратор


//chaining метод в стримах
Source -> Intermediate methods (lazy): обрабатывают стрим и на аутпуте у них тоже стрим; не работают пока не вызовем терминальный метод -> Terminal method (eager);

//concat
делает конкатенацию

//distinct
возвращает стрим уникальных элементов, проверяет при помощи метода equals

//count
считает количество элементов которое содержит стрим
возвращает Long

//peek
содержит Consumer, возвращает не void а стрим
нужен чтобы посмотреть как поэтапно проходит method chaining

//flatMap
нужно поработать не с самими элементами нашей коллекции, а с элементами элементов нашей коллекции

//collect
нужен чтобы преобразовать поток обратно в коллекцию
groupingBy - создает коллекцию по заданному параметру
partitioningBy - делать коллекцию по принципу true-false

//findFirst;
возвращает первый элемент стрима  findFirst().get();

//min, max

//limit
ограничевает количество элементов в стриме

//skip
пропускает n элементов в стриме

//mapToInt, mapToDouble, mapToLong
возвращает поток типа Integer
boxed() конвертирует int в Integer, Long, Double

//parallel stream
многопоточность в стримах
list.parallelStream().
Stream<T> s = Stream.of(...);
s.parallel().